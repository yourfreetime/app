import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  userArea: { flexDirection: "row", marginBottom: 20 },
  userImage: { width: 80, height: 80, borderRadius: 100, marginRight: 16 },
  userName: { marginBottom: 5, fontWeight: "900", fontSize: 25 }
});

export default style;
