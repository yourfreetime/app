import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  userArea: { alignItems: "center" },
  userImage: { width: 80, height: 80, borderRadius: 100, marginRight: 10 },
  userName: { marginVertical: 16, fontWeight: "900", fontSize: 20 }
});

export default style;
