import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  userImage: { borderRadius: 100 },
  small: { width: 20, height: 20 },
  regular: {
    width: 40,
    height: 40
  },
  large: {
    width: 80,
    height: 80
  }
});

export default style;
